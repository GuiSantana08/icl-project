let
    x = 5
in
    if x > 3 then
        println(x)
    else
        println(3)
    end
;
let
    x = 10
    y = 20
in
    if x < y then
        if x < 15 then
            println(x)
        else
            println(15)
        end
    else
        println(y)
    end
;;