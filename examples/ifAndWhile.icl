let
    x = 5
in
    if (x > 3) then
        print("x is greater than 3")
    else
        print("x is not greater than 3")
    end
;
let
    x = 10
    y = 20
in
    if x < y then
        if x < 15 then
            println("x is less than 15 and less than y")
        else
            println("x is more than 15 and less than y")
        end
    else
        prinntln("x is more than y")
    end
;
let
    sum = 0
in
    while sum <= 10 do
        sum = sum + i
    end
    print(sum)
;;